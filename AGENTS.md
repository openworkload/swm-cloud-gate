# Agent instructions — swm-cloud-gate

Guidance for coding agents working on this repository. The codebase is primarily **Python**.

## Tests

* Run static checks:
1. Start dev container from swm-core directory with `make cr` command.
2. Go to swm-cloud-gate directory and run `make check`
